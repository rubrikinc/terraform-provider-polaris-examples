locals {
  manifests = [
    for v in split("---", var.manifest) : yamldecode(v)
  ]
}

resource "kubernetes_manifest" "part_0" {
  for_each = { for v in try([local.manifests[0]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest = each.value
}

resource "kubernetes_manifest" "part_1" {
  for_each   = { for v in try([local.manifests[1]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_0]
}

resource "kubernetes_manifest" "part_2" {
  for_each   = { for v in try([local.manifests[2]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_1]
}

resource "kubernetes_manifest" "part_3" {
  for_each   = { for v in try([local.manifests[3]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_2]
}

resource "kubernetes_manifest" "part_4" {
  for_each   = { for v in try([local.manifests[4]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_3]
}

resource "kubernetes_manifest" "part_5" {
  for_each   = { for v in try([local.manifests[5]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_4]
}

resource "kubernetes_manifest" "part_6" {
  for_each   = { for v in try([local.manifests[6]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_5]
}

resource "kubernetes_manifest" "part_7" {
  for_each   = { for v in try([local.manifests[7]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_6]
}

resource "kubernetes_manifest" "part_8" {
  for_each   = { for v in try([local.manifests[8]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_7]
}

resource "kubernetes_manifest" "part_9" {
  for_each   = { for v in try([local.manifests[9]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_8]
}

resource "kubernetes_manifest" "part_10" {
  for_each   = { for v in try([local.manifests[10]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_9]
}

resource "kubernetes_manifest" "part_11" {
  for_each   = { for v in try([local.manifests[11]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_10]
}

resource "kubernetes_manifest" "part_12" {
  for_each   = { for v in try([local.manifests[12]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_11]
}

resource "kubernetes_manifest" "part_13" {
  for_each   = { for v in try([local.manifests[13]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_12]
}

resource "kubernetes_manifest" "part_14" {
  for_each   = { for v in try([local.manifests[14]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_13]
}

resource "kubernetes_manifest" "part_15" {
  for_each   = { for v in try([local.manifests[15]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_14]
}

resource "kubernetes_manifest" "part_16" {
  for_each   = { for v in try([local.manifests[16]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_15]
}

resource "kubernetes_manifest" "part_17" {
  for_each   = { for v in try([local.manifests[17]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_16]
}

resource "kubernetes_manifest" "part_18" {
  for_each   = { for v in try([local.manifests[18]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_17]
}

resource "kubernetes_manifest" "part_19" {
  for_each   = { for v in try([local.manifests[19]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_18]
}

resource "kubernetes_manifest" "part_20" {
  for_each   = { for v in try([local.manifests[20]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_19]
}

resource "kubernetes_manifest" "part_21" {
  for_each   = { for v in try([local.manifests[21]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_20]
}

resource "kubernetes_manifest" "part_22" {
  for_each   = { for v in try([local.manifests[22]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_21]
}

resource "kubernetes_manifest" "part_23" {
  for_each   = { for v in try([local.manifests[23]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_22]
}

resource "kubernetes_manifest" "part_24" {
  for_each   = { for v in try([local.manifests[24]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_23]
}

resource "kubernetes_manifest" "part_25" {
  for_each   = { for v in try([local.manifests[25]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_24]
}

resource "kubernetes_manifest" "part_26" {
  for_each   = { for v in try([local.manifests[26]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_25]
}

resource "kubernetes_manifest" "part_27" {
  for_each   = { for v in try([local.manifests[27]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_26]
}

resource "kubernetes_manifest" "part_28" {
  for_each   = { for v in try([local.manifests[28]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_27]
}

resource "kubernetes_manifest" "part_29" {
  for_each   = { for v in try([local.manifests[29]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_28]
}

resource "kubernetes_manifest" "part_30" {
  for_each   = { for v in try([local.manifests[30]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_29]
}

resource "kubernetes_manifest" "part_31" {
  for_each   = { for v in try([local.manifests[31]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_30]
}

resource "kubernetes_manifest" "part_32" {
  for_each   = { for v in try([local.manifests[32]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_31]
}

resource "kubernetes_manifest" "part_33" {
  for_each   = { for v in try([local.manifests[33]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_32]
}

resource "kubernetes_manifest" "part_34" {
  for_each   = { for v in try([local.manifests[34]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_33]
}

resource "kubernetes_manifest" "part_35" {
  for_each   = { for v in try([local.manifests[35]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_34]
}

resource "kubernetes_manifest" "part_36" {
  for_each   = { for v in try([local.manifests[36]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_35]
}

resource "kubernetes_manifest" "part_37" {
  for_each   = { for v in try([local.manifests[37]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_36]
}

resource "kubernetes_manifest" "part_38" {
  for_each   = { for v in try([local.manifests[38]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_37]
}

resource "kubernetes_manifest" "part_39" {
  for_each   = { for v in try([local.manifests[39]], []) : lower(format("%s-%s%s%s", v.kind, try(v.metadata.namespace, ""), can(v.metadata.namespace) ? "-" : "", v.metadata.name)) => v }
  manifest   = each.value
  depends_on = [kubernetes_manifest.part_38]
}
